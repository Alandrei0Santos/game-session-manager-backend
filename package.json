{
  "name": "game-session-manager",
  "description": "A serverless application to manage and retrieve game sessions using AWS Lambda, API Gateway, and DynamoDB.",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.398.0",
    "@aws-sdk/lib-dynamodb": "^3.398.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "aws-sdk-client-mock": "^2.0.0",
    "jest": "^29.7.0"
  },
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testMatch": [
      "**/__tests__/**/*.[jt]s?(x)",
      "**/?(*.)+(spec|test).[jt]s?(x)"
    ],
    "moduleFileExtensions": [
      "js",
      "json"
    ]
  }
}
